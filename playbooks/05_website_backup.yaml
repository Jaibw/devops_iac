---
- hosts: all
  tasks:
    - name: creating backup file
      shell: tar cfz /tmp/html.tar.gz  /var/www/html
      become: yes
    - name: Specifying a destination path
      fetch:
        src: /tmp/html.tar.gz
        dest: /home/ubuntu/backup/{{ inventory_hostname }}/
        flat: yes